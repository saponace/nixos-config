{ ... }:

{ 
  stylix.targets = {
    alacritty.enable = true;
    firefox = {
      enable = true;
      profileNames = [ "default" ];
    };
    gtk.enable = true;
    qt.enable = true;
    noctalia-shell.enable = true;
  };
}
